name: Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: true
        type: choice
        options:
          - patch
          - minor

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  release:
    name: Release
    runs-on: macos-latest
    outputs:
      version: ${{ steps.new_version.outputs.version }}

    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Cache Kotlin Native
        uses: actions/cache@v4
        with:
          path: ~/.konan
          key: ${{ runner.os }}-konan-${{ hashFiles('**/*.gradle.kts') }}
          restore-keys: |
            ${{ runner.os }}-konan-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Read current version
        id: current_version
        run: |
          source version.properties
          echo "major=$VERSION_MAJOR" >> $GITHUB_OUTPUT
          echo "minor=$VERSION_MINOR" >> $GITHUB_OUTPUT
          echo "patch=$VERSION_PATCH" >> $GITHUB_OUTPUT
          echo "Current version: $VERSION_MAJOR.$VERSION_MINOR.$VERSION_PATCH"

      - name: Calculate new version
        id: new_version
        run: |
          MAJOR=${{ steps.current_version.outputs.major }}
          MINOR=${{ steps.current_version.outputs.minor }}
          PATCH=${{ steps.current_version.outputs.patch }}

          if [ "${{ inputs.bump }}" = "minor" ]; then
            MINOR=$((MINOR + 1))
            PATCH=0
          else
            PATCH=$((PATCH + 1))
          fi

          VERSION="$MAJOR.$MINOR.$PATCH"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "major=$MAJOR" >> $GITHUB_OUTPUT
          echo "minor=$MINOR" >> $GITHUB_OUTPUT
          echo "patch=$PATCH" >> $GITHUB_OUTPUT
          echo "New version: $VERSION"

      - name: Update version.properties
        run: |
          cat > version.properties << EOF
          VERSION_MAJOR=${{ steps.new_version.outputs.major }}
          VERSION_MINOR=${{ steps.new_version.outputs.minor }}
          VERSION_PATCH=${{ steps.new_version.outputs.patch }}
          EOF
          cat version.properties

      - name: Build XCFramework
        run: ./gradlew :timeago-kmp:assembleTimeAgoKMPReleaseXCFramework

      - name: Zip XCFramework
        run: |
          cd timeago-kmp/build/XCFrameworks/release
          zip -r TimeAgoKMP.xcframework.zip TimeAgoKMP.xcframework
          echo "XCFRAMEWORK_PATH=$(pwd)/TimeAgoKMP.xcframework.zip" >> $GITHUB_ENV

      - name: Calculate checksum
        id: checksum
        run: |
          CHECKSUM=$(swift package compute-checksum ${{ env.XCFRAMEWORK_PATH }})
          echo "checksum=$CHECKSUM" >> $GITHUB_OUTPUT
          echo "Checksum: $CHECKSUM"

      - name: Update Package.swift
        run: |
          VERSION="${{ steps.new_version.outputs.version }}"
          CHECKSUM="${{ steps.checksum.outputs.checksum }}"

          cat > Package.swift << EOF
          // swift-tools-version:5.9
          import PackageDescription

          let package = Package(
              name: "TimeAgoKMP",
              platforms: [
                  .iOS(.v14),
                  .macOS(.v12)
              ],
              products: [
                  .library(
                      name: "TimeAgoKMP",
                      targets: ["TimeAgoKMP"]
                  )
              ],
              targets: [
                  .binaryTarget(
                      name: "TimeAgoKMP",
                      url: "https://github.com/samuolis/timeago-kmp/releases/download/v${VERSION}/TimeAgoKMP.xcframework.zip",
                      checksum: "${CHECKSUM}"
                  )
              ]
          )
          EOF

          cat Package.swift

      - name: Update README.md version
        run: |
          VERSION="${{ steps.new_version.outputs.version }}"

          # Update Maven Central version in README
          sed -i '' "s/timeago-kmp:[0-9]*\.[0-9]*\.[0-9]*/timeago-kmp:${VERSION}/g" README.md

          # Update SPM version in README
          sed -i '' "s/from: \"[0-9]*\.[0-9]*\.[0-9]*\"/from: \"${VERSION}\"/g" README.md

          echo "Updated README.md with version ${VERSION}"
          grep -n "timeago-kmp" README.md | head -5

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add version.properties Package.swift README.md
          git commit -m "Release v${{ steps.new_version.outputs.version }}"
          git push

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.new_version.outputs.version }}
          name: v${{ steps.new_version.outputs.version }}
          body: |
            ## TimeAgo KMP v${{ steps.new_version.outputs.version }}

            ### Installation

            **Kotlin/Android (Maven Central):**
            ```kotlin
            implementation("io.github.samuolis:timeago-kmp:${{ steps.new_version.outputs.version }}")
            ```

            **Swift Package Manager:**
            Add `https://github.com/samuolis/timeago-kmp` in Xcode Package Dependencies.

            **npm (JavaScript/TypeScript):**
            ```bash
            npm install timeago-kmp
            ```

            ### Checksums
            - XCFramework: `${{ steps.checksum.outputs.checksum }}`
          files: ${{ env.XCFRAMEWORK_PATH }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to Maven Central
        env:
          ORG_GRADLE_PROJECT_mavenCentralUsername: ${{ secrets.SONATYPE_USERNAME }}
          ORG_GRADLE_PROJECT_mavenCentralPassword: ${{ secrets.SONATYPE_PASSWORD }}
          ORG_GRADLE_PROJECT_signingInMemoryKey: ${{ secrets.SIGNING_KEY }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyId: ${{ secrets.SIGNING_KEY_ID }}
          ORG_GRADLE_PROJECT_signingInMemoryKeyPassword: ${{ secrets.SIGNING_PASSWORD }}
        run: |
          ./gradlew publishAllPublicationsToMavenCentralRepository \
            -PVERSION_NAME=${{ steps.new_version.outputs.version }} \
            --no-daemon \
            --no-configuration-cache

      - name: Build npm package
        run: ./gradlew :timeago-kmp:fixNpmPackageName --no-daemon

      - name: Upload npm package
        uses: actions/upload-artifact@v4
        with:
          name: npm-package
          path: timeago-kmp/build/dist/js/productionLibrary/

  publish-npm:
    name: Publish to npm
    needs: release
    runs-on: ubuntu-latest

    permissions:
      contents: read
      id-token: write

    steps:
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Verify package
        run: |
          cd npm-package
          cat package.json
          ls -la

      - name: Publish to npm
        run: |
          cd npm-package
          npm publish

      - name: Release Summary
        run: |
          echo "## npm Release Complete!" >> $GITHUB_STEP_SUMMARY
          echo "Published: \`timeago-kmp@${{ needs.release.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
